# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Drizzle
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/coba"

# Next Auth
# You can generate a new secret on the command line with:
# openssl rand -base64 32
# https://next-auth.js.org/configuration/options#secret
NEXTAUTH_SECRET="something-secret"
NEXTAUTH_URL="http://localhost:3000"
# Next Public Environment
NEXT_PUBLIC_API_URL="http://localhost:3000"

# forget password
SMTP_HOST="smtp.example.com"
SMTP_PORT="465"
# Akun google hidupin two step verification terus tambahin app password
SMTP_USER="alpha.faucet1@gmail.com"
SMTP_PASS="cdrnqocexhxctsql"
NEXT_PUBLIC_WS_URL="ws://localhost:3001"
NEXT_PUBLIC_BUCKET_API_KEY="web-social"
GIT_ACCESS_TOKEN=""
# Redis for Event Emitter
REDIS_URL="redis://localhost:6379"
WS_PORT=8080

# Digital Ocean env
DO_ACCESS_KEY=""
DO_SECRET_KEY=""
DO_REGION=""
DO_ORIGIN_ENDPOINT=""
DO_BUCKET_NAME=""
