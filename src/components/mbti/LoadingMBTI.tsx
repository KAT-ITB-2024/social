import { LoadingSpinner } from '../Loading';

export default function LoadingMBTI() {
  return (
    <div className="flex flex-col mt-36 justify-center items-center">
      <LoadingSpinner size={100} />
    </div>
  );
}
